# Core
numpy>=1.24
opencv-python>=4.8

# Talk to the PS Move controllers over Bluetooth (LED colour + IMU).
hidapi>=0.14

# Optional: PS3 Eye via libusb (Windows needs the libusb / WinUSB driver).
# pseyepy is installed from source: pip install git+https://github.com/bensondaled/pseyepy

# Optional: keyboard output for osu! mode on Linux / macOS (Windows uses SendInput directly).
pynput>=1.7; sys_platform != "win32"

# Tests
pytest>=7
